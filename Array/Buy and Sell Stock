class Solution {
    public int maxProfit(int[] prices) {
        int min=prices[0];
        int max=0;

        for(int val:prices){
            min=Math.min(min,val);
            int profit=val-min;
            max=Math.max(max,profit);
        }
        return max;
    }
}
